# batch-chapter-workflow.md — 批量章节处理工作流（已被 delegate-yaml-writing.md 取代）

> **此文档已过时。** 新的标准做法见 [delegate-yaml-writing.md](delegate-yaml-writing.md)。
> 核心变化：不再只用 `skeleton`，必须注入 `build-prompt` 输出到 delegate_task context。
> 保留此文件仅防止断裂链接。

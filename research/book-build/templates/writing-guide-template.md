# 第{N}章写作指南

> 当前配置了 {BOOK_COUNT} 本参考教材：
> `python3 -c "from book_config import Config; c=Config(project_root='...'); [print(f'{i}. {b[\"author\"]}《{b[\"display_name\"]}》(priority={b[\"priority\"]})') for i,b in enumerate(c.source_books,1)]"`

## 一、研读分析

### 各教材章节对应关系

| 编号 | 作者 | 书名 | 第{N}章标题 | 匹配度 | 参考策略 |
|:----:|:-----|:-----|:-----------|:------:|:---------|
| 1 | | | | 高/中/低 | 直接使用/借鉴手法/仅参考数据 |
| 2 | | | | 高/中/低 | 同上 |
| 3 | | | | 高/中/低 | 同上 |
| … | | | | | |

### 各教材写作手法对比表

对比维度对所有教材统一适用，列数 = 教材数量：

| 对比维度 | 教材1 | 教材2 | 教材3 | 教材4 | … |
|:---------|:------|:------|:------|:------|:-:|
| 章首引入方式 | | | | | |
| 概念定义深度 | | | | | |
| 公式数量和使用 | | | | | |
| 案例数量和类型 | | | | | |
| 表格使用 | | | | | |
| 历史叙事 | | | | | |
| 工程实用导向 | | | | | |
| 该章独特亮点 | | | | | |

### 各书最值得借鉴的3个手法

对每本教材分别填写（用 `book_config.py` 获取实际书名）：

**{教材1 作者}《{教材1 书名}》**：
1.
2.
3.

**{教材2 作者}《{教材2 书名}》**：
1.
2.
3.

（根据实际教材数量增减条目）

### 各教材共同盲区（发挥空间）

1. **{盲区1}** — 所有书都{具体问题}，我们可以{具体发挥}
2. **{盲区2}** — 所有书都{具体问题}，我们可以{具体发挥}
3. **{盲区3}** — 所有书都{具体问题}，我们可以{具体发挥}

---

## ⚠️ 格式规范（创作前必读）

创作时请严格遵守以下格式，避免写完后大量返工。

### 公式写法
- **行内公式**：用单 `$` 包裹，如 `$E = mc^2$`
- **块级公式**：用 `$$` 包围，编号 `\tag{章-序号}` **独占一行**，放在闭合 `$$` **之前**
  ```markdown
  $$
  IL = 20 \log(U_1/U_2)
  \tag{7-4}
  $$
  ```
- **引用块内公式**：每行加 `>` 前缀
  ```markdown
  > $$
  > IL = 20 \log(U_1/U_2)
  > \tag{7-4}
  > $$
  ```

### Mermaid 图写法
- **只能用** `graph TD`（竖向）或 `graph LR`（横向）
- **禁止使用**：`timeline`、`mindmap`、`%%{init}%%`、`<-->`（双向箭头）、`subgraph`（跨边界连接）
- **禁止在标签中使用**：emoji（`🔄` `⚠️` etc）、星号（`⭐`）、特殊 Unicode
- 节点标签用双引号包裹：`A["标签文字"]`
- 简单连接用 `A --> B`，虚线用 `A -.-> B`

### 表格写法
- 必须带对齐标记：`|:--|:--:|--:|`
- 表头用粗体 **表章-序号：标题**

### 禁止写入的内容
- ❌ `## 本章写作说明`、`12条军规落实检查`、`全章核心公式总结`
- ❌ Bloom 分类标签（`**记忆层**`、`**理解层**`）

---

## 本章定位

全章目标和写作思路。例如：本章在全书中扮演的角色、与其他章节的关系、预期读者从本章学到什么。

## ⚠️ 写作原则：借鉴手法，不照搬内容

参考教材是学习写作手法的**老师**，不是摘抄内容的**仓库**。

**正确做法**：
- ✅ 阅读各教材对应章节，理解其**写作逻辑**（为什么先讲这个再讲那个、用什么案例引出概念）
- ✅ 参考其**数据、标准号、定义原文**（事实性知识可以引用并注明出处）
- ✅ 将各教材内容**融合提炼**，用自己的语言重新组织

**错误做法**：
- ❌ 从某一教材直接复制段落或整节内容
- ❌ 仅把各教材内容"拼接"在一起，没有自己的分析视角
- ❌ 案例从参考教材摘抄（必须来自公开报道的真实事件）

**检验标准**：写成的内容应看起来像"一位教授理解了多本书后自己写的"，而非"从几本书里剪贴出来的"。

## 学习目标与内容覆盖映射

每一条学习目标必须能被正文内容覆盖。填写本章的学习目标后，逐条确认：

| # | 学习目标 | 对应的正文节号 | 落实情况 |
|:-:|:---------|:--------------:|:--------:|
| 1 | [目标1内容] | §X.X, §X.X | 必填 |
| 2 | [目标2内容] | §X.X, §X.X | 必填 |
| 3 | [目标3内容] | §X.X, §X.X | 必填 |
| 4 | [目标4内容] | §X.X, §X.X | 必填 |
| 5 | [目标5内容] | §X.X, §X.X | 必填 |

> 每一条学习目标必须至少对应一个章节。如果某目标没有对应的正文内容，则需补充对应章节或修改学习目标。

## 结构建议

| 大纲节号 | 标题 | 建议字节数 | 主导手法 | 主要素材来源 |
|:--------|:----|:---------:|:---------|:------------|
| N.1 | | | | |
| N.2 | | | | |
| N.3 | | | | |
| N.4 | | | | |
| N.5 | | | | |
| **总计** | | | | |

> **体量目标**：第1章={第1章字节}KB → 本章目标={第1章字节×5~10}KB

---

## 四、每节写作指南

### N.1 {标题}

**写作手法**：{动态：根据该节内容选择最合适的教材手法}

**引入方式**：{建议从什么现象/问题说起}

**必须包含的要素**：
- {要素1}（来源：{哪本教材}）
- {要素2}（来源：{哪本教材}）
- {要素3}（需要原创的）

**建议的设问过渡**：
- {小节A}→{小节B} 之间："{具体的设问句}"
- {小节B}→{小节C} 之间："{具体的设问句}"

**案例建议**：
- {案例主题} — 使用{哪本教材的什么素材}，按三段式（经过+分析+启示）展开

---

## 五、总体建议

### 5.1 素材清单

分别列出从每本教材提取的可用素材（通过 `grep_all_books()` 获取）：

**{教材1}**：
-

**{教材2}**：
-

**{教材3}**：
-

（根据实际教材数量增减）

### 5.2 12条军规落实检查（Agent自查用，不写入教材正文）

> ⚠️ 以下军规是 Agent 写完本章后自查自纠用的，**不是教材正文内容**。
- [ ] 详实案例驱动：具体时间/地点/数字/分析
- [ ] 历史极致细节：具体日期+组织全称+里程碑
- [ ] 系统清晰结构：学习目标+对比表+分层结构
- [ ] 四阶段演进脉络：萌芽→形成→体系化
- [ ] 从现象到概念：日常现象→正式定义→深度案例
- [ ] 工程实用导向：配工程场景和具体措施
- [ ] 大篇幅讲细讲透：每节达到目标字节数
- [ ] 含数字实例：每节至少1个具体计算
- [ ] 多标准并列定义：核心概念处
- [ ] 概念对比表：易混淆概念处
- [ ] 三级案例结构：经过+分析+启示
- [ ] 教学设问过渡：节间≥2处

#### 公式格式检查
所有公式和`\tag{}`必须遵守：
- `\tag{N-M}`独占一行，紧跟下一行`$$`
- `$$`独立一行（公式内容不在`$$`同一行）
- 行内公式用`$E=mc^2$`，块级用`$$...$$`
- 违反则运行 `post_generation_check.py --fix` 自动修复
